[project]
name = "momo-sim-world"
version = "0.1.0"
description = "Genesis world server runtime for momo /sim (managed with uv)"
requires-python = ">=3.10"
dependencies = [
    "genesis-world>=0.2.0",
    "numpy>=1.24",
]

[tool.uv]
# Pure runtime scripts — not a package to publish.
package = false